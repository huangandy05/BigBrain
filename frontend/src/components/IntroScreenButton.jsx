import { Button } from '@mui/material';
import { styled } from '@mui/material/styles';

const IntroScreenButton = styled(Button)({
  width: '300px',
  height: '30px',
  borderRadius: '20px',
  margin: '7px',
  padding: '1px 8px',
});

export default IntroScreenButton;
